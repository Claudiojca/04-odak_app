import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.touch.odak',
  appName: 'Odak',
  webDir: 'www',
  bundledWebRuntime: true
};

export default config;
